name: Exness Candidate OOS

on:
  push:
    branches: [feature/tickscalper-rawtick-v1-20260927]
    paths:
      - "scripts/exness_candidate_oos.py"
      - ".github/workflows/exness-candidate-oos.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  oos:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: nautilus/cache/exness-ticks
          key: exness-xauusd-2026-09-v1
          restore-keys: |
            exness-xauusd-
      - name: Run frozen OOS
        run: python3 -u scripts/exness_candidate_oos.py
      - uses: actions/upload-artifact@v4
        with:
          name: exness-candidate-oos
          path: results/exness-candidate-oos/
          if-no-files-found: error
          retention-days: 30
